/** Filter and sort keys for admin booking queries. */

export const FILTER_KEYS = Object.freeze({
    ALL:      "all",
    PENDING:  "pending",
    VERIFIED: "verified",
    REJECTED: "rejected"
});

export const SORT_KEYS = Object.freeze({
    NEWEST: "newest",
    OLDEST: "oldest"
});
